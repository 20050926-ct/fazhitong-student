node_modules/
build/
coverage/
.DS_Store
*.log

# 环境变量（敏感信息，不要上传公开仓库）
.env
.env.local
.env.*.local
!.env.example

# 本地临时文件
temp/
.hbuilderx/
cloudflared.exe

# 注意：data/*.json 不再忽略，
# 这些是种子数据（法律文章、题库、视频等），Render 首次部署必需。
# 如需部署后增量更新，接入 Firebase 即可持久化。
